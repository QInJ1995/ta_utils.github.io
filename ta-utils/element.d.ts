/**
 * DOM元素信息
 * @category DOM元素操作
 * @import import {getHeight,getWidth,getStyle} from '@yh/ta-utils/element'
 */

/**
 *
 * @param dom
 * @param innerWidth
 */
export function getHeight(dom: Node, innerWidth: number): string;

export function getWidth(dom: Node, innerHeight: number): string;

export function getStyle(dom: Node): string;
