import moment from 'moment/src/moment';
import 'moment/src/locale/zh-cn';
export { moment, };
/**
 * 日期转换成moment日期格式
 * @method getMoment
 * @param {String,moment} value 目标串
 * @param {String} format 格式
 */
export default function getMoment(value?: unknown, format?: string): unknown;
export { getMoment };
